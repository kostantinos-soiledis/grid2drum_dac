# Results

This folder keeps the paper-facing result artifacts:

- generated TeX snippets and tables
- normalized CSV/JSON metric files
- acoustic-evaluation summary files and per-clip metric CSVs
- lightweight comparison metadata used by the paper

It intentionally excludes target-audio caches, prediction export trees, raw
tensor shards, and long-running log files. Those remain in the original
experiment workspace.
