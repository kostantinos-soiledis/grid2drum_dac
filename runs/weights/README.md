# Weights

The required demo weights are committed through Git LFS. They should be present after cloning the repository with Git LFS enabled.

If `python app.py` reports missing or pointer-only files, install Git LFS and fetch the LFS objects for this repository.

